[build]
  command = "pip install -r requirements.txt"
  publish = "templates"

[[redirects]]
  from = "/*"
  to = "/.netlify/functions/app"
  status = 200

[functions]
  included_files = ["templates/**", "database.db"]